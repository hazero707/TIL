package days05;

import java.util.Scanner;

public class Ex02_02 {

	public static void main(String[] args) {
		// 한 문자를 입력받아서
		 //  숫자, 알파벳(대,소문자), 한글, 특수문자(#$!%@)
		//   입니다라고 출력하세요.
		
		char one;
		
		//BufferedReader br
		//Scanner scanner
		try (Scanner scanner = new Scanner(System.in)){
			
			System.out.print("> 한 문자 입력 ? ");
			//scanner.next(); //String
			//scanner.nextInt(); Int
			//scanner.nextByte(); Byte
			//scanner.nextBoolean(); Boolean
			//scanner.nextDouble(); Double"
			//scanner.nextshort(); short"
			//scanner.nextFloat(); Float
			//scanner.nextLong(); Long   /scanner char 없음 구분자 구별하기 힘들어서인듯 추측
			
			// "a" ->'a' 변환  x  //문자를 문자로 변환 할수 없음 
			//"a" ->'a' 변환
		}
		/*
		String inputData = scanner.next(); //"A"
		one = inputData.charAt(0); //'a'
		
		if('0' <= one&&one <='9') {             //숫자조건식
			System.out.println("숫자");
		}else if ('a' <=one  &&  one<='z' || ('A' <= one && one<='z') {   //알파벳(대소)조건식)
			System.out.println("알파벳");
		}else if (한글조건식) {
			한글
		}
		else if (one =='#' || one =='$' ||one =='$ ||one =='$) {   //특수문자
			System.out.println("특수문자");
			System.out.println(x);
		
		*/
		}
			
		
		
		

	}

